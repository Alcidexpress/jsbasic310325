// Crear un array con los números
let numeros = [1, 2, 3, 4];

// Usar filter para crear un nuevo array con solo los números pares
let numerosPares = numeros.filter(numero => numero % 2 === 0);

// Mostrar el array con los números pares en la consola
console.log("Números pares:", numerosPares);

