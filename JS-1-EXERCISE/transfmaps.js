// Crear un array con los números
let numeros = [1, 2, 3];

// Usar map para crear un nuevo array con el cuadrado de cada número
let cuadrados = numeros.map(numero => numero * numero);

// Mostrar el nuevo array con los cuadrados en la consola
console.log("Cuadrados de los números:", cuadrados);
