// Crear un array con los números
let numeros = [5, 1, 3];

// Usar sort con una función de comparación para ordenar de forma ascendente
numeros.sort((a, b) => a - b);

// Mostrar el array ordenado en la consola
console.log("Array ordenado de forma ascendente:", numeros);
