// Inicializar el contador en 5
let contador = 5;

// Usar un bucle while para hacer el contador regresivo
while (contador >= 1) {
    console.log(contador);
    contador--;  // Decrementar el contador en cada iteración
}
